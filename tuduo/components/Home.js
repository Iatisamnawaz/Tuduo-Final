import React from 'react';
import ScreenNavigation from './ScreenNaviation';
import Home from ''
// Showing the TabNavigation to show all 4 screens
export default (Home = ({ navigation }) => {
	return <ScreenNavigation />;
});
